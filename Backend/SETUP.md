# Setup

This one-off migration guide has been superseded — its steps (adding
`utils/api.ts`, replacing the inline base64 `ImageUploadField`, tagging uploads
by category) are all applied in the codebase now.

See **[README.md](README.md)** for setup, scripts, the full API reference, the
data model, and the deployment checklist.
