# Base URL of the AUSTPC backend API.
# Optional in local development — the app falls back to http://localhost:5000/api.
# Set this when deploying, e.g. https://api.austpc.example.com/api
VITE_API_BASE_URL=http://localhost:5000/api
