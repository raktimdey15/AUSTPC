{
  "name": "austpc",
  "version": "1.0.0",
  "private": true,
  "description": "AUSTPC website - React frontend served by an Express/MongoDB backend",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "npm ci --omit=dev --prefix Backend && npm ci --include=dev --prefix Frontend && npm run build --prefix Frontend",
    "start": "node Backend/server.js",
    "seed": "npm run seed --prefix Backend",
    "dev:db": "npm run db --prefix Backend",
    "dev:api": "npm run dev --prefix Backend",
    "dev:web": "npm run dev --prefix Frontend",
    "setup": "npm ci --prefix Backend && npm ci --prefix Frontend"
  }
}
